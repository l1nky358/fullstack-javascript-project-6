extends ../layout

block content
  .d-flex.justify-content-between.align-items-center.mb-4
    h1 Статусы задач
    if user
      a.btn.btn-primary(href='/statuses/new') Создать статус

  if flash && flash.success
    .alert.alert-success.alert-dismissible.fade-show(role='alert')
      = flash.success
      button.btn-close(type='button' data-bs-dismiss='alert')
  
  if flash && flash.error
    .alert.alert-danger.alert-dismissible.fade-show(role='alert')
      = flash.error
      button.btn-close(type='button' data-bs-dismiss='alert')

  if statuses && statuses.length > 0
    table.table.table-striped
      thead
        tr
          th ID
          th Название
          if user
            th Действия
      tbody
        each status in statuses
          tr
            td= status.id
            td= status.name
            if user
              td
                a.btn.btn-sm.btn-primary(href=`/statuses/${status.id}/edit`) Редактировать
                form.d-inline(action=`/statuses/${status.id}?_method=DELETE` method='POST')
                  input(type='hidden' name='_method' value='DELETE')
                  button.btn.btn-sm.btn-danger.ms-2(type='submit' onclick='return confirm("Вы уверены?")') Удалить
  else
    p.text-muted Статусы отсутствуют